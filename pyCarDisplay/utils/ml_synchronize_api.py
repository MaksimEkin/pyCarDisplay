from PIL import Image, ImageDraw, ImageFont

class MLDataSynch():

	# need to pass a frame dictionary that contains dictionaries of image paths and detected image lists
	def __init__(o):
		o.frame = 0

	def MLDataSynch():


            #cropped_images = self.ml_synchronize_api.synch(detected_dictionary["box_info"], depth_image)
